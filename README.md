# NeuralNetwork
This is a simple PyTorch experiment where I learn to use PyTorch to create a Neural Network. 
The NN would then predict a flower type based on 4 features including: "sepal_length",	"sepal_width",	"petal_length", and 	"petal_width".
